swagger: "2.0"
info:
  title: "v1 API"
  version: "1.0"
basePath: "/v1"
schemes:
  - "https"
paths:
  /health:
    get:
      produces:
        - "application/json"
      responses:
        "200":
          description: "Success"
      x-amazon-apigateway-integration:
        type: mock
        passthroughBehavior: when_no_match
        requestTemplates:
          application/json: '{"statusCode": 200}'
        responses:
          default:
            statusCode: "200"
            responseTemplates:
              application/json: '{"version": "v1", "status": "healthy"}'
  /v1/resource:
    get:
      produces:
        - "application/json"
      responses:
        "200":
          description: "Success"
      x-amazon-apigateway-integration:
        type: mock
        passthroughBehavior: when_no_match
        requestTemplates:
          application/json: '{"statusCode": 200}'
        responses:
          default:
            statusCode: "200"
            responseTemplates:
              application/json: '{"version": "v1", "data": "resource"}'
