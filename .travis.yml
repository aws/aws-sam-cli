# Enable container based builds
sudo: required
language: python

services:
  - docker

install:
  # Install the code requirements
  - make init

# Install Docs requirements

script:
  # Runs unit tests
  - tox
  # tox has trouble with running our integ-test target in Py3. We run integ-test here until the cause of the issue
  # is addressed.
  - make integ-test
